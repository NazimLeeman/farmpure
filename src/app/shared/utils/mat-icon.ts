export const matIconUrl = (
	icon: string,
	size = "24px",
	format = "svg",
): string => {
	return `https://fonts.gstatic.com/s/i/short-term/release/materialsymbolsoutlined/${icon}/default/${size}.${format}`;
};
